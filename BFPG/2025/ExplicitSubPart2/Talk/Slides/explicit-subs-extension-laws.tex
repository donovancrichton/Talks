\only<1>{
\Def{Equational Theory: Extension Laws}
    {
     \vspace*{-0.3cm}
     \[ \delta \circ (\gamma, M : \alpha) = 
          (\delta \circ \gamma), M[\delta]  \]
     \vspace*{-0.3cm}
    }
\Ex{Extension Composition}
    {Composition distributes into extensions.}
}

\only<2>{
\Def{Equational Theory: Extension Laws}
    {
     \vspace*{-0.3cm}
     \[ (\gamma, M : \alpha) \circ \delta = (\gamma \circ \delta, M[\delta] : \alpha) \]
     \vspace*{-0.3cm}
     \[ id = (\uparrow, 1 : \alpha) \]
     \vspace*{-0.3cm}
    }
\Ex{Identity Decomposition}
    {Identity as shift extended with variable.}
}

